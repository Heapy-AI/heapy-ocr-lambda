# 재검증 체크박스 오류 인계

- 작성자: 김진우
- 작성일: 2026-09-08
- 기준: 사용자 재업로드 화면, 첫 번째 일반검진 PDF의 검사표 로컬 대조
- 로컬 기준 커밋: d56ddb6. 해당 커밋의 배포 성공 화면은 이전 대화에서 확인했다.
- 제한: 이번 작업의 Lambda 실행 버전·원시 응답·작업 식별자는 확인하지 못했다.

## 확인된 문제

혈압 분리와 여러 수치의 표준 연결은 개선됐다. 그러나 종합판정 선택지·안내 문장이 검사로
남았으며, 실시대상 여부의 표시가 결과값으로 들어갔다. 원본상 비해당인 선택검사에서 결과가
반환된 사례도 있다. 검진기관에는 문서 제목이 표시됐고, 혈색소 항목은 검수 화면에 없었다.
원본에 표시된 기관 판정도 여러 수치 검사에서 누락됐다. 개인 식별정보와 실제 검사값은 기록하지 않는다.

이는 미매칭 코드만 보완해 해결할 수 없다. 특히 원본의 빈 결과 선택지를 정상으로 읽는 오류는
실제 검사 수행 여부와 결과 선택을 분리해야 한다. 일반 정성 청력은 기존 마스터의 주파수 수치
검사와 달라 별도 협의가 여전히 필요하다.

## 이번 로컬 보완

1. 실시대상 여부 `eligibility`와 실제 결과 근거 `result_evidence`를 내부 Gemini 스키마에 추가했다.
   비해당·대상 불명확 또는 결과 근거 불명확인 항목을 통과시키지 않는다.
2. `체크`, `해당`, `선택됨` 등 결과 내용이 아닌 표시는 서버 검증에서도 제외한다.
   수치가 명확한데 판정만 `체크`로 반환된 경우에는 수치는 보존하고 판정을 null로 둔다.
3. 종합판정 선택지·검사 수검 안내 문장을 개별 검사 목록에서 제외한다.
4. 결과통보서·보고서 등의 문서 제목은 기관명으로 반환하지 않고 null로 둔다.
5. 모델 지침에 빈 사각형과 채워진 선택지, 실시대상과 오른쪽 결과 열의 구분,
   수치 행 누락 점검 및 기관 판정 확인을 추가했다.

공개 API·DB·마스터·프런트는 변경하지 않았다. 내부 메타데이터가 없는 레거시 입력은 기존
검증 경로를 유지하며, 새 Gemini 요청 스키마에서는 새 필드가 필수다.

## 검증과 남은 작업

- 합성 테스트 151개 및 Ruff 검사 통과.
- 원본 문서를 외부 API로 전송하지 않았다. 임시 화면 분할 이미지와 원본 렌더링은 삭제했다.
- 이번 변경은 로컬에만 있으며 push·배포하지 않았다.
- 모델이 실제 비해당 표시를 해당이라고 잘못 분류하면 메타데이터 필터만으로 원본 오류를
  증명할 수 없다. 혈색소 누락과 기관 판정 복원도 이번 단위 테스트로 해결을 보장하지 않는다.
- 배포 후 같은 문서를 새로 업로드하여 실제 검사 행 누락, 비해당 행 유입, 기관 판정 누락을
  함께 확인해야 한다. 경고 개수 감소만을 성공 기준으로 사용하지 않는다.
- 흉부 검사 등 미매칭은 촬영 방법의 의미를 확인한 뒤 코드 연결을 검토한다. 이번에는 경고를
  줄이기 위한 추정 별칭이나 DB 마스터를 추가하지 않았다.

## 참조

Reference 폴더는 없다. 공개 필드·기관 판정 보존·확정 저장 책임은 기존에 확인한 아래 문서를 따른다.

- `C:/Users/jinwo/heapy-backend/docs/api/HEAPY_BACKEND_API_명세_v1.md`
- `C:/Users/jinwo/heapy-backend/docs/architecture/HEAPY_전체_시스템_아키텍처_v1.md`
- `C:/Users/jinwo/heapy-backend/docs/requirements/HEAPY_화면검토_확정결과.md`
- `C:/Users/jinwo/heapy-backend/docs/database/HEAPY_DB_개발기준서_v1.md`
- `OCR_다양한_검진양식_지원_인계.md`
