# 검진 저장 제외 안내와 개선 로그 변경 계약

- 작성자: 김진우
- 사용자 승인: 600초 유지, 확인 필요 항목은 기본 접기·자동 제외, 프런트 UI/UX 수정과 OCR 개선 로그 요청

## 화면과 저장

reviewRequired는 '이번 저장에 포함되지 않는 항목 N개'로 표시하며 기본 접힘 상태다. 펼치면 기존 원문 후보와 분류·매칭·추출 사유를 보여준다. 모든 값이 틀렸거나 중복이라고 단정하지 않는다. 사용자의 항목별 제외 체크를 요구하지 않는다. 원문을 보고 일반 결과·소견을 수정하는 기존 검수 기능은 유지한다.

확정 시 원본 스냅샷의 reviewRequired.fieldKey를 excludedFieldKeys에 자동 포함한다. 일반 검사와 소견은 기존 수동 제외를 유지한다. 같은 검사 코드의 상충값을 임의 선택하지 않는다. 서버의 식별자·누락·소유권·트랜잭션 검증은 그대로 유지한다. 자동 제외는 UI 정책이며 API의 명시적 제외 목록은 계속 전송한다. 모든 저장 대상이 비면 저장하지 않는다. 생성부터 600초 TTL은 변경하지 않는다.

## 개선 로그

OCR 완료 시 버전, 페이지 수, 배열별 건수, 제외 후보 사유별 건수 및 값 없는 형식 특징의 집계만 기존 CloudWatch 로그에 기록한다. 실제 사용자 확정 여부가 아니라 'OCR이 반환한 저장 제외 후보' 지표로 명명한다. 요청별 작업 ID는 기존 운영 로그와 같은 식별자를 사용한다. 마스터 스냅샷 해시로 배포별 집계를 대조할 수 있게 한다.

검사값·소견·이름·파일명·경로·원문 해시는 기록하지 않는다. 이 집계는 회귀·실패 유형 비중 추적에 사용하며 원문 기반 학습 자료는 아니다. 원문 사례를 장기 수집하려면 별도 동의·비식별화·보존기간·삭제 정책을 갖춘 수집 설계가 필요하다. 현재 원본 삭제·임시 결과 TTL·로그 보존 정책은 유지한다. 서버가 이미 기록하는 확정 제외 식별자·remove 메타데이터도 유지한다.

## 참조

Reference/rule 및 프로젝트 AGENTS.md 파일은 없어 대화 규칙을 적용했다. 실제 참조는 docs/api/검진_저장확장_공유계약.md, docs/api/검진_버전2_DB적용_개발배포_결과.md와 기존 API 명세·시스템 아키텍처·요구사항·DB 설계 대조 목록이다. 이번 승인으로 공유 계약의 '확인 필요 항목마다 사용자가 제외를 선택'하는 UI 정책을 위와 같이 대체한다. API 필드·버전·마이그레이션은 변경하지 않는다.
