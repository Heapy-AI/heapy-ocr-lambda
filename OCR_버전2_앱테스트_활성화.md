# OCR 버전 2 앱 테스트 활성화

- 작성자: 김진우
- 작성일: 2026-09-09
- 사용자 승인: 앱 테스트 준비 및 OCR dev 푸시

## 활성화 범위

검진 처리 진입점에서 schemaVersion=2 결과를 생성한다. 내부 작업 봉투의 contractVersion=1.0은 유지한다. CHECKUP_SCHEMA_VERSION 미설정 시 2이며, 명시적 1은 구버전 결과 경로를 선택한다. 1·2 외 설정은 오류로 종료한다. 복약 처리 계약은 유지한다. checkupContexts 및 버전 3은 구현하지 않는다.

공유 DB 마이그레이션 20260908085751 적용 후 조회한 마스터 161개를 catalog.py에 동기화했다. 코드·이름·단위·value_type·활성 여부를 보존한다. HEARING_GENERAL_LEFT·HEARING_GENERAL_RIGHT·CHEST_XRAY 추가와 CHEST_XRAY_PA=text를 반영했다. 혈압·감마지티피·평가 항목의 새 코드는 없다. 촬영 방향이 없는 흉부 촬영을 PA에 연결하지 않으며 일반 청력을 1000Hz dB 검사로 바꾸지 않는다. 정성 마스터의 numericValue는 null이다.

## 앱 준비 확인

C:/Users/jinwo/heapy-frontend 로컬에 소견 검수·수정·명시적 제외·버전 2 확정·상세 조회 코드가 존재한다. 타입 검사와 관련 4개 테스트 묶음 33개가 통과했다. 프런트 변경은 기존 미커밋 상태 그대로 보존했다. 프런트 코드를 원격에 푸시하거나 메트로를 실행하지 않았다. 사용자는 해당 로컬 프로젝트의 메트로를 직접 실행한다.

OCR 합성 테스트 184개 및 ruff 검사가 통과했다. 실제 처리 함수의 기본 버전 2·명시적 버전 1·잘못된 버전 차단·복약 경로를 검증했다. 실제 폰 업로드·확정·600초 후 상세 조회는 사용자 기기에서 이어서 확인할 대상이다.

## 폰 테스트 순서

1. 현재 heapy-frontend 로컬 코드로 메트로를 실행하고 개발 앱을 연결한다.
2. 검진 등록에서 PDF를 새로 업로드한다. 이전 작업은 기존 스냅샷 또는 만료 상태일 수 있으므로 재사용하지 않는다.
3. 개별 검사, 검사별 소견, 종합소견이 구분되는지 확인한다.
4. 수치·소견 본문을 직접 수정해 본다. 확인 필요 및 상충 결과는 원문을 보고 이번 저장에서 제외할 항목을 명시적으로 선택한다.
5. 저장 완료 후 상세 화면에서 수정한 결과와 소견을 확인한다.
6. 업로드 생성 시각에서 600초가 지나도 정식 기록 상세가 조회되는지 확인한다.

폰에서 오래된 설치 앱을 메트로 없이 실행하면 새 로컬 프런트가 반영되지 않을 수 있다. 환경변수·네이티브 구성이 바뀐 설치본이라면 개발 앱 재빌드가 필요할 수 있다. 구버전 앱은 새 검수 형식 지원 여부에 따라 저장이 거부될 수 있다.

## 복구와 참조

OCR 배포 실패 시 기존 배포 스크립트의 live 별칭 롤백을 따른다. 배포 후 문제 시 검증된 이전 버전으로 되돌리되 새 작업만 구버전 생성 대상으로 삼는다. 이미 생성·확정된 버전 2 결과를 삭제하거나 구버전으로 재해석하지 않는다. DB 되돌림을 OCR 롤백의 전제조건으로 삼지 않는다.

OCR·백엔드·프런트 프로젝트에 Reference/rule 및 AGENTS.md 파일은 발견되지 않았다. 백엔드 docs/api/검진_저장확장_공유계약.md, docs/api/검진_버전2_DB적용_개발배포_결과.md 및 해당 문서에 나열된 API 명세·아키텍처·요구사항·DB 설계 문서를 기준으로 대조했다. 기존 모델 비교 및 무관한 미커밋 변경은 배포에서 제외한다. 실제 건강 문서·키·응답 원문은 포함하지 않는다.
