# 다양한 검진 양식 지원 인계

- 작성자: 김진우
- 작성일: 2026-09-08
- 상태: 로컬 구현·합성 테스트 완료. 이번 변경의 배포 및 실제 문서 외부 OCR 검증은 미실행.

## 1. 근거와 책임 경계

프로젝트에 AGENTS.md, Reference 및 Reference/rule 폴더가 없다. 대화에서 제공된 협업 규칙을
적용한다. 다음 백엔드 문서와 기존 OCR 인계서를 기준으로 공개 요청·응답을 유지한다.

- `C:/Users/jinwo/heapy-backend/docs/api/HEAPY_BACKEND_API_명세_v1.md`
- `C:/Users/jinwo/heapy-backend/docs/api/HEAPY_BACKEND_API_검토확정결과.md`
- `C:/Users/jinwo/heapy-backend/docs/architecture/HEAPY_전체_시스템_아키텍처_v1.md`
- `C:/Users/jinwo/heapy-backend/docs/requirements/HEAPY_화면검토_확정결과.md`
- `C:/Users/jinwo/heapy-backend/docs/database/HEAPY_DB_개발기준서_v1.md`
- `C:/Users/jinwo/heapy-backend/docs/database/HEAPY_DB_물리설계_v1.md`
- `백엔드_OCR_Lambda_연동_인계서.md`

API 예시의 소문자 코드보다 2026-09-08 배포 계약의 실제 마스터 코드 사용 지침을 우선한다.
Lambda는 파일 검증·변환·추출·매칭·임시 결과만 담당하고 DB에 접근하지 않는다.
백엔드·프런트 프로젝트 및 실제 DB는 수정하지 않았다.

## 2. 지원 범위

특정 병원 양식에 종속하지 않고, 일반검진·종합검진 문서의 신체계측, 혈압, 시력, 청력,
혈액, 소변 및 흉부 X선의 기재된 검사 결과를 추출 대상으로 한다.
마스터와 의미·단위가 맞는 실제 검사만 코드에 연결한다. 실제 검사지만 매칭이 불확실하면
미매칭으로 남긴다. 안내 문장이나 질환 분류를 검사명으로 바꾸지 않는다.

종합소견, 위험평가, 연도별 그래프, 별도 암검진, 내시경·조직검사·초음파·CT·MRI 상세 소견은
현재 개별 검사 저장 범위에서 제외한다. 종합소견 저장은 아래 협업 제안이며 구현되지 않았다.
사용자가 업로드한 문서의 모든 내용을 보존한다고 안내해서는 안 된다.

사용자가 제공한 세 문서는 로컬에서 전체 페이지 구성을 확인했다.
개인 이름·문서 내용·실제 검사값은 이 문서와 테스트에 복제하지 않는다.

| 합성 검증 유형 | 원본에서 확인한 구조적 차이 | 처리 기준 |
|---|---|---|
| 일반검진 통보서 | 병합된 질환 분류·검사명·결과·체크 판정 | 실제 검사 행과 선택된 판정만 사용 |
| 기관별 종합검진 | 여러 페이지에 걸친 검사 표와 상세 소견 혼재 | 문서 제목으로 제외하지 않고 영역 구분 |
| 비교표 포함 문서 | 현재·과거 열, 그래프, 통보서의 반복 결과 | 명시적인 현재 결과만 추출, 동일 결과 정리 |

## 3. 구현 변경

- `general_checkup.py`: 종합검진 일괄 제외 문구 제거, 공통 검사 범위 및 비교표 지침 추가.
- `gemini.py`: 내부 항목에 `result_period=current/previous/unknown`을 요청한다.
  이전·불명확 결과는 서버 필터에서 제외한다. `advanced`, `trend` 영역도 제외한다.
  내부 필드는 공개 API로 전달하지 않는다. 기존 내부 항목 구조와의 호환을 위해 필드가 없는
  레거시 입력은 기존 검증을 거친다. 새 Gemini 요청 스키마에서는 이 필드가 필수다.
- `rule_parser.py`: 명시적인 금회 결과 열만 선택한다. 중복 결과 머리글, 날짜만 있는 열,
  빈 현재 결과 및 열 관계가 사라진 비교표는 추정하지 않는다. 영역·페이지 경계에서 상태를
  초기화한다. 검진일은 표시된 검사일에 한정하고 충돌 시 null로 둔다.
- 동일 의미의 명시적인 한영 병기 별칭을 실제 로컬 마스터 코드·단위와 대조해 추가했다.
  일반 혈당을 공복혈당으로, 정성 청력을 주파수 수치 검사로 연결하지 않는다.
- `service.py`: 페이지별 검진일이 다르면 첫 날짜를 선택하지 않고 null로 반환한다.
  동일 코드·값·단위·판정의 중복 정리와 충돌 보존은 기존 동작을 유지한다.

Gemini 분류는 모델의 인식에 의존한다. 서버 필터가 원본 표의 의미를 독립적으로 증명하는 것은
아니다. Vision의 평문 fallback에는 셀 좌표가 없으므로 모든 병합 머리글·영문 제목을 복원할 수
없다. 합성 구분자 표 테스트 성공이 실제 Vision 표 복원 성공을 뜻하지 않는다.

## 4. 유지한 계약

- 공개 결과는 `measuredAt`, `providerName`, `items`이며 새 필드를 추가하지 않았다.
- `itemCode`는 기존 실제 마스터 코드 또는 null이다. confidence는 근거 없이 생성하지 않는다.
- 기관 판정과 실제 단위를 보존하고 진단·점수·공복 상태를 추정하지 않는다.
- 실행·조회·정리 계약 버전 1.0, 원본 처리 후 삭제, 작업 생성부터 600초 TTL을 유지한다.
- 20,000,000바이트, PDF 20페이지 제한과 비밀번호 없는 파일 정책을 유지한다.
- 마스터 미매칭 결과는 현 계약상 확정에서 제외해야 한다. 이를 자동으로 숨기지 않는다.
- 같은 코드의 충돌 결과는 현재 DB의 회차·코드 유일성 제약 때문에 사용자 선택이 필요하다.

## 5. 백엔드·프런트 협업 제안

이 절은 확정된 API·DB 변경이 아니다. 백엔드와 프런트에서 함께 검토 후 적용해야 한다.

1. 마스터의 실제 의미에 맞춰 numeric/text/composite를 정리한다. 앞선 읽기 전용 DB 조회에서는
   158개 항목이 모두 numeric이었다. 이것만으로 미매칭 원인을 설명할 수는 없다.
2. 일반 청력 좌·우 등 정성 검사 코드를 협의한다. 기존 1000Hz 수치 코드에 연결하지 않는다.
3. 짧은 정성 결과는 기존 `value`와 null `numeric_value`로 저장한다. 모든 문자열을 JSONB로
   이동할 필요는 없다. 수정 이력과 확정 검증은 백엔드 책임이다.
4. 종합소견은 검진 회차에 연결된 별도 영역을 설계한다. 문장만 필요하면 text, 판정·권고 등을
   구분하면 버전이 있는 JSONB 구조를 검토한다. 임의 필드·컬럼·DDL은 추가하지 않았다.
5. 기관 원문 소견의 사용자 확정값을 먼저 보존하고 AI 요약은 별도의 선택 기능으로 설계한다.
   전체 OCR 원문·파일 경로를 업무 DB에 저장하는 방식은 사용하지 않는다.
6. 검수 UI는 검사 결과와 소견을 분리하고, 지원 범위 및 미지원 영역을 설명해야 한다.
   현재 공개 응답에는 영역별 제외 내역이 없으므로 동적인 제외 안내를 하려면 API 협의가 필요하다.
7. 날짜가 불명확하거나 충돌하면 사용자가 검진일을 확인한다. 서로 다른 검진 회차가 섞인 PDF의
   자동 분할·여러 회차 저장은 이번 범위가 아니다.

## 6. 검증 결과와 배포 전 확인

- `python -m pytest -q`: 139개 통과. 기존 복약·파일 검증·임시 저장 및 정리 테스트 포함.
- `python -m ruff check .`: 통과.
- 새 테스트는 합성 값만 사용하며 모델 응답을 모의했다. 실제 건강 문서를 외부 API로 보내지 않았다.
- 현재·과거 열 위치 변경, 현재 빈 값, 모호한 머리글, 참고치·소견 제외, 정성 결과, 날짜 충돌,
  페이지별 반복·충돌 결과, 15페이지 호출 분할을 검증했다.
- 이번 변경은 로컬 상태이며 GitHub push·개발 배포를 수행하지 않았다.

실제 배포 검증은 별도 단계다. 새 버전 식별 후 세 문서 유형을 원본과 대조하여 누락, 오매칭,
과거값 혼입, 판정·단위 오류 및 처리 시간을 확인해야 한다. 앱 검수·확정 저장까지 확인한다.
15페이지를 한 페이지씩 처리하는 모의 테스트는 600초 내 실제 완료를 보장하지 않는다.
시간이 부족하면 TTL을 몰래 늘리지 않고 호출 시간 측정 후 병렬 처리·페이지 선별 등의 후속
설계를 결정한다. 이미지 및 키·토큰·검사값을 일반 로그에 남기지 않는다.
